$kind: http-request
description: Resend OTP to patient email. Use this if OTP expires or user didn't receive it.
url: "{{baseUrl}}/api/auth/resend-otp"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{patientEmail}}"
    }
order: 6000
