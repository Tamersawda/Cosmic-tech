$kind: http-request
description: Should return 400 Bad Request (password < 6 chars)
url: "{{baseUrl}}/api/auth/register"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "weak@example.com",
      "password": "123",
      "userType": "patient",
      "fullName": "Weak Password User"
    }
order: 15000
